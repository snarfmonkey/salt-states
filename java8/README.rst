Oracle Java 8
================

A simple state to install java 8 on ubuntu, pre-seeding the agreement
for automation's sake.

The set-as-default state uses another ubuntu oracle package
to reset java8 as the default version if it is changed.
